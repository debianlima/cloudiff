# Runtime

Configurações por nó e contratos declarativos.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `config/runtime`

Configurações por nó e contratos declarativos.

| Item | Tipo | Finalidade |
|---|---|---|
| [`artifact-executor.env.example`](artifact-executor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`certificate-monitoring.json`](certificate-monitoring.json) | `.json` | Configuração, inventário, evidência ou estado serializado em JSON. |
| [`forja-agent.env.example`](forja-agent.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`machine-agent.env.example`](machine-agent.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`policy-signing.pub`](policy-signing.pub) | `.pub` | Arquivo de suporte da plataforma. |
| [`preview-executor.env.example`](preview-executor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`production-canary.env.example`](production-canary.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`production-homologation-executor.env.example`](production-homologation-executor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`production-public.env.example`](production-public.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
