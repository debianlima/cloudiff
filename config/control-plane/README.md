# Control Plane

Configurações por nó e contratos declarativos.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `config/control-plane`

Configurações por nó e contratos declarativos.

| Item | Tipo | Finalidade |
|---|---|---|
| [`academic-audit.env.example`](academic-audit.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`agent-registry.env.example`](agent-registry.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`approvals.env.example`](approvals.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`build-broker.env.example`](build-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`certificate-monitoring.json`](certificate-monitoring.json) | `.json` | Configuração, inventário, evidência ou estado serializado em JSON. |
| [`cloudif-authz-gate.env.example`](cloudif-authz-gate.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-supabase-session.env.example`](cloudif-supabase-session.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-tenant-guard.env.example`](cloudif-tenant-guard.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`control-dashboard.env.example`](control-dashboard.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`control-plane.env.example`](control-plane.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`deployment-broker.env.example`](deployment-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`evaluations.env.example`](evaluations.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`machine-admin-security.env.example`](machine-admin-security.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`machine-agent.env.example`](machine-agent.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`machine-controller-db.env.example`](machine-controller-db.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`mcp-gateway.env.example`](mcp-gateway.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`monitor.env.example`](monitor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`node-execution-policy.json`](node-execution-policy.json) | `.json` | Política ou configuração declarativa em JSON. |
| [`node24-homologation.json`](node24-homologation.json) | `.json` | Configuração, inventário, evidência ou estado serializado em JSON. |
| [`notifications.env.example`](notifications.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`policy-signing.pub`](policy-signing.pub) | `.pub` | Arquivo de suporte da plataforma. |
| [`portal-qa.env.example`](portal-qa.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`portal.env.example`](portal.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`preview-broker.env.example`](preview-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`preview-cleanup.env.example`](preview-cleanup.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`production-targets.json`](production-targets.json) | `.json` | Configuração, inventário, evidência ou estado serializado em JSON. |
| [`project-capabilities-policy.json`](project-capabilities-policy.json) | `.json` | Política ou configuração declarativa em JSON. |
| [`project-onboarding.env.example`](project-onboarding.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`runtime-policy.json`](runtime-policy.json) | `.json` | Política ou configuração declarativa em JSON. |
| [`storage-guard.env.example`](storage-guard.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`supabase-launch.env.example`](supabase-launch.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`supabase-onboarding-broker.env.example`](supabase-onboarding-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`supabase-release-agent.env.example`](supabase-release-agent.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`transaction-reconciler.env.example`](transaction-reconciler.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`workspace-broker.env.example`](workspace-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
