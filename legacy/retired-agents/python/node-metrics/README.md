# Agente Python aposentado — NÃO IMPLANTAR

Este diretório preserva somente a última implementação Python histórica de `cloudif-node-metrics` para auditoria e rollback forense.

Desde U26, o agente ativo é o binário C++ `cloudif-node-metrics-cpp`, construído a partir de `src/node_metrics/main.cpp`. Não copie este `.py` para `/usr/local/sbin`, não habilite serviço Python e não trate este arquivo como implementação canônica.
