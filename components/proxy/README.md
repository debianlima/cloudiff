# Proxy

Componentes implantados no host de proxy e publicação.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/proxy`

Componentes implantados no host de proxy e publicação.

| Item | Tipo | Finalidade |
|---|---|---|
| [`current-apps/`](current-apps/) | Diretório | Componentes implantados no host de proxy e publicação. |
| [`etc/`](etc/) | Diretório | Componentes implantados no host de proxy e publicação. |
| [`srv/`](srv/) | Diretório | Componentes implantados no host de proxy e publicação. |
| [`usr/`](usr/) | Diretório | Componentes implantados no host de proxy e publicação. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
