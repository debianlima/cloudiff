# Multiservice Deployment Executor Current

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/runtime/current-apps/multiservice-deployment-executor-current`

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

| Item | Tipo | Finalidade |
|---|---|---|
| [`cloudif-multiservice-deployment-executor.py`](cloudif-multiservice-deployment-executor.py) | `.py` | Implementa `DeploymentError`, `canonical`, `db`, `init_db`, `run`, `docker` e outros componentes. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
