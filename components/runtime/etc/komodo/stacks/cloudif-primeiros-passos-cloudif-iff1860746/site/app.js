const out=document.getElementById("db-result");document.getElementById("db-test").addEventListener("click",async()=>{out.textContent="Consultando o Supabase...";try{const u=window.CLOUDIF_CONFIG.supabaseRest+"/cloudif_tutorial_steps?select=id,title,completed&order=id";const r=await fetch(u,{headers:{apikey:window.CLOUDIF_CONFIG.anonKey,Authorization:"Bearer "+window.CLOUDIF_CONFIG.anonKey}});const text=await r.text();if(!r.ok)throw new Error("HTTP "+r.status+" "+text);out.textContent=JSON.stringify(JSON.parse(text),null,2)}catch(e){out.textContent="Falha: "+e.message}});
