# Etc

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/runtime/etc`

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

| Item | Tipo | Finalidade |
|---|---|---|
| [`cloudif/`](cloudif/) | Diretório | Componentes implantados no host de runtime, Forgejo, Komodo e executores. |
| [`komodo/`](komodo/) | Diretório | Componentes implantados no host de runtime, Forgejo, Komodo e executores. |
| [`systemd/`](systemd/) | Diretório | Componentes implantados no host de runtime, Forgejo, Komodo e executores. |
| [`cloudif-multiservice-deployment-executor.env.example`](cloudif-multiservice-deployment-executor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-multiservice-preview-executor.env.example`](cloudif-multiservice-preview-executor.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
