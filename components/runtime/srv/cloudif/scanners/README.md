# Scanners

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/runtime/srv/cloudif/scanners`

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

| Item | Tipo | Finalidade |
|---|---|---|
| [`images.env.example`](images.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`node-distroless-images.env.example`](node-distroless-images.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`node-images.env.example`](node-images.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
