# Cloudif

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/runtime/srv/cloudif`

Componentes implantados no host de runtime, Forgejo, Komodo e executores.

| Item | Tipo | Finalidade |
|---|---|---|
| [`scanners/`](scanners/) | Diretório | Componentes implantados no host de runtime, Forgejo, Komodo e executores. |
| [`test-fixtures/`](test-fixtures/) | Diretório | Componentes implantados no host de runtime, Forgejo, Komodo e executores. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
