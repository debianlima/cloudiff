# Agent Controller Current

Componentes implantados no plano de controle e no host de hospedagem.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/control-plane/current-apps/agent-controller-current`

Componentes implantados no plano de controle e no host de hospedagem.

| Item | Tipo | Finalidade |
|---|---|---|
| [`cloudif-agent-controller-run.sh`](cloudif-agent-controller-run.sh) | `.sh` | Automação Shell operacional da plataforma. |
| [`cloudif-agent-controller.py`](cloudif-agent-controller.py) | `.py` | Implementa `now`, `env`, `api`, `atomic`, `partition`, `coalesce` e outros componentes. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
