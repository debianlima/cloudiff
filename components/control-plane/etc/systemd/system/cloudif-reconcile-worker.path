[Unit]
Description=CloudIF reconciliation queue watcher

[Path]
PathChanged=/var/lib/cloudif/reconcile-queue/incoming
MakeDirectory=true
Unit=cloudif-reconcile-worker.service

[Install]
WantedBy=multi-user.target
