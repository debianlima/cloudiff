# Etc

Componentes implantados no plano de controle e no host de hospedagem.

<!-- CLOUDIFF-AUTO-DOC:BEGIN -->

## Inventário automático de `components/control-plane/etc`

Componentes implantados no plano de controle e no host de hospedagem.

| Item | Tipo | Finalidade |
|---|---|---|
| [`cloudif/`](cloudif/) | Diretório | Componentes implantados no plano de controle e no host de hospedagem. |
| [`systemd/`](systemd/) | Diretório | Componentes implantados no plano de controle e no host de hospedagem. |
| [`cloudif-multiservice-preview.env.example`](cloudif-multiservice-preview.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-project-config-controller.env.example`](cloudif-project-config-controller.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-project-config-reconciler.env.example`](cloudif-project-config-reconciler.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |
| [`cloudif-supabase-mcp-broker.env.example`](cloudif-supabase-mcp-broker.env.example) | `.example` | Modelo de variáveis de ambiente; não deve conter segredos reais. |

> Esta seção é gerada por `scripts/generate-directory-readmes.py`. Conteúdo manual fora dos marcadores é preservado.

<!-- CLOUDIFF-AUTO-DOC:END -->
